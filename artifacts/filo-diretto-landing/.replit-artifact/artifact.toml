kind = "web"
previewPath = "/"
title = "Filo Diretto"
version = "1.0.0"
id = "artifacts/filo-diretto-landing"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 25217

[services.development]
run = "pnpm --filter @workspace/filo-diretto-landing run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/filo-diretto-landing", "run", "build" ]
publicDir = "artifacts/filo-diretto-landing/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "25217"
BASE_PATH = "/"
